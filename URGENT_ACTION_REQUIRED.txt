═══════════════════════════════════════════════════════════════════
  URGENT: FIREBASE PRIVATE KEY SECURITY FIX REQUIRED
═══════════════════════════════════════════════════════════════════

GitGuardian detected an exposed Firebase private key in your GitHub repo.

CRITICAL STEPS (Do these NOW):

1. REVOKE THE EXPOSED KEY
   ----------------------
   • Go to: https://console.firebase.google.com
   • Select project: raghost-port
   • Go to: Project Settings → Service Accounts
   • Click: "Manage Service Account Permissions"
   • Find: firebase-adminsdk-fbsvc@raghost-port.iam.gserviceaccount.com
   • Click 3 dots → Manage Keys
   • DELETE the exposed key
   • Click "Add Key" → "Create New Key" → "JSON"
   • Download the new key file

2. UPDATE RENDER ENVIRONMENT VARIABLES
   -----------------------------------
   • Open the downloaded JSON file
   • Go to: https://dashboard.render.com
   • Select your backend service
   • Go to: Environment tab
   • Update these 3 variables with values from the JSON:
     - FIREBASE_PROJECT_ID
     - FIREBASE_PRIVATE_KEY
     - FIREBASE_CLIENT_EMAIL
   • Save changes (Render will auto-redeploy)

3. UPDATE LOCAL .env FILE
   ----------------------
   • Update backend/.env with the new key values
   • NEVER commit this file (it's already in .gitignore)

4. MARK AS FIXED IN GITGUARDIAN
   ----------------------------
   • Reply to the GitGuardian email
   • Click "Mark as fixed" or "False positive"

═══════════════════════════════════════════════════════════════════

WHAT I'VE ALREADY DONE FOR YOU:

✓ Installed git hook to prevent future commits of sensitive files
✓ The hook blocks: .env files, firebase JSON, private keys
✓ Run "setup-security.ps1" on any new clone to install the hook

═══════════════════════════════════════════════════════════════════

TIME REQUIRED: ~10 minutes
PRIORITY: CRITICAL

Once you complete steps 1-3, your app will be secure again!
