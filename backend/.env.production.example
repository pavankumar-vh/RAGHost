# Production Environment Configuration
# Copy these to your Render environment variables

# Server Configuration
NODE_ENV=production
PORT=5001

# ============================================
# MEMORY OPTIMIZATION (Choose One Option)
# ============================================

# OPTION 1: NORMAL MODE (Recommended for paid plans or >512MB RAM)
# - Full performance with Redis, queues, 50 DB connections
# - Required RAM: ~600-800MB
# Use these settings:
ENABLE_LOW_MEMORY=false
ENABLE_CACHING=true
ENABLE_QUEUES=true
ENABLE_COMPRESSION=true

# OPTION 2: LOW MEMORY MODE (For Render Free Tier 512MB)
# - Reduced performance but fits in 512MB
# - 5 DB connections, no Redis/queues, lighter compression
# Use these settings instead:
# ENABLE_LOW_MEMORY=true
# ENABLE_CACHING=false
# ENABLE_QUEUES=false
# ENABLE_COMPRESSION=true

# Clustering (Usually not needed)
ENABLE_CLUSTERING=false

# MongoDB Atlas Connection
# MONGODB_URI=your_mongodb_uri_here

# Encryption Key
# ENCRYPTION_KEY=your_encryption_key_here

# Firebase Admin SDK Configuration
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_PRIVATE_KEY=your_private_key
# FIREBASE_CLIENT_EMAIL=your_client_email

# CORS Origins
CORS_ORIGINS=https://rag-host.vercel.app,http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Redis Configuration (Optional - Disable for 512MB environments)
# REDIS_URL=your_redis_url_here
