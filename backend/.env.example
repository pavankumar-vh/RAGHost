# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Connection (REQUIRED)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/chatbot-hosting?retryWrites=true&w=majority

# Encryption Key (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your_32_byte_hex_key_here

# Firebase Admin SDK (Path to your service account JSON)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===================================
# SCALABILITY & PERFORMANCE OPTIONS
# ===================================

# Redis Configuration (OPTIONAL - but HIGHLY RECOMMENDED for production)
# Benefits: Caching, distributed rate limiting, job queues
# Free tier available at: https://redis.com/try-free/ or https://upstash.com/
# Leave empty to run without Redis (will reduce performance under load)
REDIS_URL=
# Example for Upstash: rediss://default:<PASSWORD>@<ENDPOINT>:6379
# Example for Redis Cloud: redis://:<PASSWORD>@<HOST>:<PORT>

# Clustering Configuration (OPTIONAL - Use all CPU cores)
# Set to 'true' to enable multi-core processing
# Significantly improves performance for high-traffic applications
ENABLE_CLUSTERING=false
MAX_WORKERS=4  # Maximum number of worker processes (defaults to CPU count)

# Rate Limiting (these are defaults, override if needed)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
